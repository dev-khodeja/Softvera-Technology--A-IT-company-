import React from 'react';
import VideoDetails from '../Components/ServiceDetails/VideoDetails';

const VideoService = () => {
    return (
        <div>
            <VideoDetails/>
        </div>
    );
};

export default VideoService;
